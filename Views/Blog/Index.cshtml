<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Blog Engine</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <!-- Bootstrap 5 CDN (No Popper.js needed here) -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* Custom Styling */
        body {
            font-family: 'Roboto', sans-serif;
            background-color: #f8f9fa;
            padding-top: 20px;
        }

        .hero-section {
            background-color: #007bff;
            color: white;
            padding: 50px 0;
            border-radius: 10px;
            margin-bottom: 30px;
            text-align: center;
        }

            .hero-section h1 {
                font-family: 'Poppins', sans-serif;
                font-size: 3rem;
            }

        .search-bar {
            max-width: 600px;
            margin: 20px auto;
            display: flex;
            justify-content: center;
        }

            .search-bar input {
                padding: 10px;
                font-size: 1rem;
                border-radius: 5px;
                border: 1px solid #ced4da;
                transition: border-color 0.3s ease-in-out;
                caret-color: #007bff; /* Change caret color to blue */
            }

                /* Focus Effect for Search Bar */
                .search-bar input:focus {
                    border-color: #007bff;
                    outline: none;
                }

            .search-bar button {
                padding: 10px 20px;
                font-size: 1rem;
                margin-left: 10px;
                border-radius: 5px;
            }

        .btn-custom {
            font-weight: 600;
            margin: 5px 10px;
            padding: 12px 20px;
            border-radius: 5px;
        }

        .navbar {
            margin-bottom: 30px;
        }

        .nav-link {
            font-weight: 600;
        }

        footer {
            text-align: center;
            margin-top: 50px;
            color: #333;
        }

            footer p {
                font-size: 0.9rem;
            }
    </style>
</head>
<body>
    <div class="container">
        <!-- Hero Section -->
        <div class="hero-section">
            <h1>Welcome to Your Blog Engine</h1>
            <p>Learn and share your knowledge through powerful, professional blogging.</p>
        </div>

        <!-- Search Bar Section -->
        <div class="search-bar">
            <input type="search" class="form-control" placeholder="Search blog posts..." aria-label="Search">
            <button class="btn btn-outline-primary" type="button">Search</button>
        </div>

        <!-- Navigation Links -->
        <div class="d-flex justify-content-center flex-wrap">
            <a href="#" class="btn btn-primary btn-custom">Home</a>
            <a href="#" class="btn btn-primary btn-custom">All Blog Posts</a>
            <a href="#" class="btn btn-primary btn-custom">View Blog Post #1</a>
            <a href="#" class="btn btn-success btn-custom">Admin Panel</a>
            <a href="#" class="btn btn-secondary btn-custom">Privacy</a>
        </div>

        <!-- Optional Footer -->
        <footer class="text-center mt-5">
            <p>&copy; 2025 Blog Engine | All Rights Reserved</p>
        </footer>
    </div>

    <!-- Bootstrap 5 JavaScript CDN (No Popper.js needed) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
